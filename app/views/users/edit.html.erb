<header>
  <%= render "navbar" %>
</header>

<%= form_for [current_user] do |f| %>
   <%= f.label :name %>
   <%= f.text_field :name %>
   <br>
   <%= f.label :email %>
   <%= f.text_field :email %>
   <br>

   <%= f.button %>
   <%= link_to 'Cancel', profile(current_user) %>
<% end %>
